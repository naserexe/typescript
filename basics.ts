const person  = {
  name: 'naser',
  age: 20,
};

console.log(person);

let users: { // we can use semicolon when declare a object without assign a value
  name: string;
  email: string;
  phone: number;
}